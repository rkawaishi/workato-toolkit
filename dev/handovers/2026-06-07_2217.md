# Handover: workato-toolkit P3a/P3b 実装完了（知識ベース移植 + skills 移植+MCP参照置換）。次は P3c

- 日時: 2026-06-07 22:17
- ブランチ: `main`（PR #2・#3 マージ済、feature ブランチ削除済）
- 直近コミット: `c9c6039 Merge pull request #3 from rkawaishi/feat/p3b-skills-migration`

## 今回やったこと
- セッション冒頭 `/handover read` で前回（P1/P2 完了時点）を復帰。**P2 の PR #1 をマージ**（codex/local code-review 実施 → バグなし）→ CC marketplace は **ローカル directory source のまま維持**（P3 開発中は remote 切替を P5 まで延期と判断）。
- **P3 を 3 サブプランに分割**（writing-plans のスコープ判断）: P3a 知識ベース / P3b skills+MCP参照 / P3c rules・agents・hooks・派生・CI。
- **P3a 実装完了 → PR #2 マージ**（Subagent-Driven: 実装 sonnet + spec✅ + code-quality✅、local /code-review 実施、follow-up を PR #2 にコメント記録）。
- **P3b 実装完了 → PR #3 マージ**（同 Subagent-Driven フロー。code-quality 指摘 2 Important+表記統一 minor を修正反映）。
- 各 PR ごとに `/codex:review`（working-tree 専用なので superpowers/ のみ対象＝コード指摘なし）と local `/code-review`（ブランチ差分）を使い分け。

## 決定事項
- **P3 は 3 分割**（P3a/P3b/P3c）。各々 feature ブランチ + PR + 2段階サブエージェントレビュー。
- **CC marketplace は当面ローカル directory source 維持**。GitHub remote への切替は **P5（リリース）まで延期**（P3 開発中の push→再install を避けるため）。
- **guides は `docs/guides/` 配下**（MCP 配信下に統一）、**glossary は `docs/i18n/`**（P3a）。
- skill の docs 参照は **`workato_docs_lookup(path)` / `workato_docs_list(prefix)`** を呼ぶ。手動 overlay（`@docs/X`+`@org/docs/X`+legacy `@projects/docs/`）は **MCP の org-wins マージ 1 呼び出しに集約**、legacy 行は削除（P3b）。
- **WRITE 系は P3b 対象外＝P4 へ**: `org/docs/` 書戻し、auto-learn/sync-connectors の kit docs 書込み（素 `docs/` 表記なので @-grep に掛からず自然分離）。
- レビュー follow-up の振り分け: **#1 旧 submodule+setup.sh の install 手順刷新＝P5**、**#2 docs/ 内部 @docs クロス参照＝P3b Task7 で対応済**（`docs/guides/quickstart-cursor.md:242` のメタ言及のみ P5 残置）。

## 捨てた選択肢と理由
- P3 を 1 つの巨大 plan にする → サブシステムが独立（docs / skills / rules+CI）なので writing-plans のスコープ指針に従い 3 分割。
- `/codex:review` で PR 差分をレビュー → codex native は **working-tree 専用**で不可。PR 差分は local `/code-review`（`main...HEAD`）を使用。
- code-review minor #3（connector-sdk 未カウント）→ **誤指摘**（テストに `connector-sdk >= 2` あり）として不採用。

## ハマりどころ
- `/codex:review` は **コミット済み差分を見ない**（working-tree のみ）。毎回 superpowers/ の未追跡 plan しか拾わず「指摘なし」。PR コードのレビューは local `/code-review` 必須。
- P3b 実装で **plan の引用 old 文字列が backtick を欠いていた**（実ファイルは `` `@docs/...` `` と backtick 包み）。実装者は実ファイルに合わせて置換し semantically 同一。spec レビューで正当と確認。
- 既存 `skills/ping`（P1 由来）が 21 番目として存在 → テストの `EXPECTED_SKILLS` に `ping` を追加（移植エラーではない）。
- Bash cwd ドリフト注意。git 操作前に `git rev-parse --show-toplevel` で repo 確認（旧 `workato-dev-kit` を誤操作しないため）。

## 学び
- docs-overlay MCP は `docs/` 配下に置くだけで配信（rglob）。P3a は**サーバ無変更**でコンテンツ追加のみで成立。
- 手動 dual-read 規約（org-knowledge-overlay）は MCP の org-wins マージに**完全に吸収**でき、skill 本文が大幅に簡潔化。
- Subagent-Driven は移植系でも有効: 実装 sonnet + spec/quality レビューで backtick 差・ping 既存などの判断を確実に検証できた。

## 次にやること
- [ ] **P3c の plan を writing-plans で作成 → Subagent-Driven 実装**。内容: 旧 `framework/claude/{rules(9),agents(1),hooks(3),settings.json}` を共有ツリー（`rules/` `agents/` `bin/` `hooks/`）へ移植 + **派生生成**（Cursor `.mdc`、Codex agent `.toml`、context `CLAUDE.md/AGENTS.md/GEMINI.md`、`hooks/{hooks,cursor.hooks,codex.hooks}.json` 3種）+ `.github/workflows/sync-check.yml`（派生ドリフト検知）。`@.claude/rules/...` 参照（create-recipe 等）も P3c で解決。移植元インベントリは旧 repo `framework/claude/` 参照。
- [ ] **P4**: 学習サイクル/WRITE 系（`/learn-*` が project `org/docs/` へ Write、auto-learn/sync-connectors の kit-docs 書込み問題＝plugin の docs は読取専用なので書込み先を org/docs へ再設計）+ 常時ルール配信（CC/Codex SessionStart hook、Cursor `.mdc`、Gemini `GEMINI.md`）。
- [ ] **P5**: `release.yml` + README install 手順（plugin 方式・4エディタ分）+ 旧 repo deprecation 告知 + **guides の旧 submodule install 記述刷新**（review #1）+ `quickstart-cursor.md:242` メタ言及整理 + **CC marketplace を GitHub remote へ切替**。
- [ ] (後) Gemini 追加（§6.2.4 実機検証）/ Codex・Cursor で P1+P2+P3 実機検証。
- [ ] **保留**: `superpowers/`（design.md+html, prfaq, p1/p2/p3a/p3b plan）は**未 commit**のまま（意図的）。commit するなら feature ブランチ + PR。

## 関連ファイル
- `superpowers/2026-06-07-workato-toolkit-plugin-distribution-design.md` — 設計 spec（§2 鍵ルール「docs 参照は MCP」、§4.5 docs-overlay MCP、§5 移植）。**未 commit**。
- `superpowers/specs/2026-06-07-workato-toolkit-plugin-distribution-prfaq.md` — PR/FAQ。**未 commit**。
- `superpowers/2026-06-07-workato-toolkit-p3a-...plan.md` / `-p3b-...plan.md` — 実装済 plan（P3c の形式参考）。**未 commit**。
- `/Users/ryotaro/workspace/workato-toolkit/docs/` — 知識ベース 362 件（P3a 移植済。connectors 317 / platform・logic・patterns・sdk / guides 13 / i18n）。
- `/Users/ryotaro/workspace/workato-toolkit/skills/` — 21 skill（P3b 移植 + MCP 参照化済。20 canonical + ping）。
- `/Users/ryotaro/workspace/workato-toolkit/mcp/docs-overlay/{overlay.py,server.py}` — docs-overlay MCP（P2。無変更）。
- `/Users/ryotaro/workspace/workato-toolkit/tests/` — test_overlay / test_manifests / test_docs_migration / test_skills_migration（計 31 PASS）。
- 移植元（凍結・read-only）: `/Users/ryotaro/workspace/workato-dev-kit/framework/claude/{rules,agents,hooks,settings.json}` — **P3c の移植元**。

# Handover: workato-toolkit P3c 実装完了（rules/agents/hooks 移植 + 派生生成器 + sync-check CI）。次は P4

- 日時: 2026-06-07 23:32
- ブランチ: `main`（PR #4 マージ済、feature ブランチ削除済）
- 直近コミット: `29fda2c Merge pull request #4 from rkawaishi/feat/p3c-rules-agents-hooks-derived`

## 今回やったこと
- セッション冒頭 `/handover read` で前回（P3a/P3b 完了時点）復帰 → **P3c の plan 作成 → Subagent-Driven 実装 → PR #4 マージ**まで完走。
- **P3c plan 作成前に範囲を AskUserQuestion で確定**: 「配信成果物まで含める」（.mdc/context/GEMINI.md/SessionStart 配線 + @.claude/rules 解決まで P3c。runtime 実機検証と学習WRITEのみ P4）。
- **plan**: `superpowers/2026-06-07-workato-toolkit-p3c-rules-agents-hooks-derived-plan.md`（全11タスク・TDD・no-placeholder）。移植元フォーマット（旧 `framework/codex/hooks.json`・`workato-builder.toml`・`scripts/sync_agents.py`）を実調査して反映。
- **実装（11タスク）**: T1 rules(9) / T2 agent / T3 hooks(3)+credential-patterns / T4-6 生成器 `scripts/sync_derived.py`（.mdc+集約 / Codex .toml / context 3種）/ T7 `bin/session-start-rules` / T8 hooks json 3種 / T9 `@.claude/rules`→ルール名(8 skill×13) / T10 manifest 配線 / T11 `sync-check.yml`。**最終 58 tests pass**。
- **2段階レビュー実施**: final-review subagent + local `/code-review`(7 finder)。本物のバグを修正（下記）。findings は PR #4 コメントに記録。
- **PR #4 マージ**（MERGEABLE/CLEAN、CI: sync-check の derived-files + tests 両方 green = `sync-check.yml` 実環境稼働確認済）。

## 決定事項
- **P3c 範囲 = 配信成果物まで**（.mdc/Codex .toml/context CLAUDE・AGENTS・GEMINI.md/hooks json 3種/`bin/session-start-rules`/@.claude/rules 解決/sync-check CI）。runtime 実機検証と学習WRITE は P4。
- **共有ツリー型なので生成器は rules/agent/context のみ**（skills は P3b で1式共有済 → per-editor skill 生成は不要）。`scripts/sync_derived.py` は旧 `sync_agents.py` の縮小版（pure helper を verbatim 流用）。
- **`@.claude/rules/X.md` は全エディタ共通で「`X` rule (always-on)」へ書換**（常時配信前提 → per-editor 書換なし＝skills 共有維持）。
- **credential-patterns.txt は repo root**、hook は `bin/` 配置。`block-credential-read.sh` の patterns 解決を `../../`→`../` に修正（logic は byte-identical）。
- **context 3ファイルは rules 起点で生成**（旧 CLAUDE.md の submodule/setup.sh 記述は排除）。`bin/session-start-rules` が AGENTS.md を SessionStart `additionalContext` で注入。
- **派生物（.mdc/.toml/context）はコミットする**（gitignore しない）。整合は `sync-check.yml` が `python3 scripts/sync_derived.py` + `git diff --exit-code` で担保。
- **manifest 配線**: CC `hooks:"./hooks/hooks.json"` / Codex `agents:"./agents/"`+`hooks:"./hooks/codex.hooks.json"` / Cursor `rules:"rules/"`+`hooks:"hooks/cursor.hooks.json"`（キー名は調査確定の最小集合・実機受理は runtime TODO）。

## 捨てた選択肢と理由
- **P3c で実装 subagent を全タスクに使う** → 実装 subagent が T2/T3 で**完全脱線**（無関係な `.claude/settings.json` allowlist 作成、T3 の必須パス修正を未適用放置）。検証で検知・復旧後、**確定コード系（T4-T11）は私が直接 TDD 実装**（指定コードが plan で完全確定済のため subagent より確実）。移植系の verbatim コピーは diff 検証で spec/quality 充足。
- **`/codex:review` で PR レビュー** → codex ネイティブは working-tree 専用でコミット済み差分を見ない（P1〜P3b で確認済）。今回も working tree は `superpowers/` のみ。ユーザー選択で **local `/code-review`（main...HEAD）** を使用。
- **agent の per-rule .mdc を alwaysApply:true にする** → 個別ルールは false、集約 `workato-project.mdc` のみ true。「always-on」は内容が常時 context にある意味（aggregate/AGENTS.md/GEMINI.md 経由）で正しい。

## ハマりどころ
- **実装 subagent の脱線**（上記）。教訓: plan でコードが完全確定している transcription 系は直接実装が速く確実。subagent は判断を要するタスク向き。
- **`@.claude/CLAUDE.md` 参照が既存2テストガードをすり抜けていた**: `test_no_at_prefixed_doc_refs_remain`(@docs系) も `test_no_claude_rules_path_refs`(@.claude/rules) も拾わず。code-review で検出 → ガードを任意 `@.claude/` に拡張。create-recipe/learn-recipe/agent の lifecycle 参照は MCP 配信の `workato_docs_lookup("guides/lifecycle.md")` へ（docs/guides/lifecycle.md は P3a 移植済）。
- **.mdc description の YAML コロン**: `workato-cli-autonomy` の見出し `...rule: do not ask...` で `description:` が不正 YAML → Cursor がルールを黙って落とす恐れ。生成器で `json.dumps` 引用化（全 .mdc 再生成）。旧 sync_agents.py も同じ未引用だった（潜在バグ）。
- **chmod が permission 拒否**された → `python3 -c "os.chmod(...)"` で回避。
- **drift テストはコミット前に必ず fail**（生成→未コミット差分を検知）。コミット後 green になる正常挙動。

## 学び
- `sync-check.yml` 方式（生成器再実行 + `git diff --exit-code`）は実 CI で機能（PR #4 で derived-files job が pass）。派生コミット + CI 整合検知は有効。
- final-review subagent + local code-review(7 finder) の二段で、verbatim コピーの見落とし（agent の stale path）と生成器の YAML バグを確実に捕捉できた。
- codex review は本リポの PR ワークフロー（コミット先行）と相性が悪い。PR コードのレビューは local `/code-review` 一択。

## 次にやること
- [ ] **P4**: 学習サイクル/WRITE系 + 常時ルール runtime 実機検証。
  - 学習WRITE: `/learn-recipe` `/learn-pattern` が project `org/docs/` へ Write、auto-learn/sync-connectors の kit-docs 書込み問題（plugin docs は読取専用 → 書込み先を org/docs へ再設計）。
  - runtime 実機検証（P3c の TODO 回収）: ① Codex SessionStart 対応有無（未対応なら `hooks/codex.hooks.json` から SessionStart 削除）② 各 manifest の hooks/agents/rules キー名の実機受理 ③ CC `${CLAUDE_PLUGIN_ROOT}` SessionStart 注入の発火 ④ Cursor `.mdc` alwaysApply + `hooks` 受理 + `./bin/` 相対解決。
- [ ] **P5**: `release.yml` + README install 手順（4エディタ）+ **`permissions.deny`/`.codexignore`/`.cursorignore`/`.geminiignore` スニペット掲載**（plugin は配れないため install docs で補完）+ Codex の file-read 保護（Bash-only guard の補完）+ 旧 repo deprecation 告知 + 旧 submodule install 記述刷新 + `docs/guides/quickstart-cursor.md:242` メタ言及 + **CC marketplace を GitHub remote へ切替**。
- [ ] (軽微 follow-up・非ブロッカー) 生成器の rule 3回読み + `sync_aggregate_mdc`/`_context_body` の demote ループ重複 → `_demoted_rule_bodies()` 抽出 / `bin/session-start-rules` fallback の realpath / Codex slash 正規表現の `-` 除外。PR #4 コメント参照。
- [ ] (後) Gemini 同居実機検証（design §6.2.4）/ Codex・Cursor で P1-P3 実機検証。
- [ ] **保留**: `superpowers/`（design/prfaq/p1-p3c plan）は**未 commit**のまま（意図的）。

## 関連ファイル
- `superpowers/2026-06-07-workato-toolkit-p3c-rules-agents-hooks-derived-plan.md` — P3c 実装 plan（形式参考）。**未 commit**。
- `superpowers/2026-06-07-workato-toolkit-plugin-distribution-design.md` — 設計 spec（§2 共有 vs エディタ別 / §4 常時ルール配信 / §4.5 docs-overlay MCP / §6.2 要設計判断）。**未 commit**。
- `scripts/sync_derived.py` — 派生生成器（rules→.mdc+集約 / agent→.toml / context 3種）。`main()` に sync_rules_mdc/sync_aggregate_mdc/sync_agent_toml/sync_context。
- `rules/*.md`(9) — canonical 常時ルール。`rules/*.mdc`(9+集約 workato-project.mdc) は生成物。
- `agents/workato-builder.md` — canonical agent。`.toml` は Codex 用生成物。
- `bin/{block-credential-read,validate-before-push,sync-docs-after-sdk-push}.sh, session-start-rules}` — hook 実体（共有）。`credential-patterns.txt`(root) を `../` で参照。
- `hooks/{hooks,codex.hooks,cursor.hooks}.json` — エディタ別 hooks 配線（plugin-root 変数・casing 差）。
- `.github/workflows/sync-check.yml` — 派生ドリフト検知 + テスト CI。
- `tests/{test_rules_agents_hooks_migration,test_derived_sync,test_manifests,test_skills_migration,test_overlay,test_docs_migration}.py` — 計 58 tests。
- 移植元（凍結・read-only）: `/Users/ryotaro/workspace/workato-dev-kit/{framework/claude,framework/codex,framework/credential-patterns.txt,scripts/sync_agents.py}` — P4/P5 の参照元（learn 系 skill・guides の install 記述等）。

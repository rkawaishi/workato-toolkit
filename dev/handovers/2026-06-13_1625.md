# Handover: P4 レビュー→マージ完了 + P5 リリース準備完了（PR #7 マージ）。次は P6 runtime 検証→リリース

- 日時: 2026-06-13 16:25
- ブランチ: `main`（PR #5・#7 マージ済、feature ブランチ削除済）
- 直近コミット: `d8a335e Merge pull request #7 from rkawaishi/feat/p5-release-prep`

## 今回やったこと
- **セッション冒頭**: `/doctor` で CLAUDE.md 49K 警告 → 構造調査。**CLAUDE.md/AGENTS.md/GEMINI.md は生成物**（`rules/*.md` 9本が真ソース、`scripts/sync_derived.py` が生成）と判明。整理は方針未決で **issue #6** 化して保留。
- **作業途中ブランチの特定**: このリポの唯一の worktree・`feat/p4-learning-write-redesign` が途中作業と判明（PR #5 OPEN）。fumi_llc 等の他リポ worktree は無関係と切り分け。
- **P4（learning-WRITE redesign）の仕上げ**: `/code-review`(high) 実施 → 実バグ修正（**create-recipe の git add が `org/docs/` を拾わず未コミットになる**、onboard L11/L102 の取りこぼし、テストガードを `docs/connectors/` 限定→全 doc tree に一般化）→ **PR #5 マージ**（CI green、64 tests）。
- **P5（リリース準備）を完走**: writing-plans でプラン作成 → subagent-driven（確定系は subagent、書換系は私が直接のハイブリッド）で実装 → `/code-review`(high) → 修正 → **PR #7 マージ**（CI green、68 tests）。
  - 成果: `release.yml`（tag 発火→derived-sync 検証→pytest→GitHub Release）/ README 全面 plugin-native 書換（CC/Cursor/Codex install + Gemini coming-soon + セキュリティスニペット）/ 3 manifest を **0.1.0** / 7ガイド+README を submodule モデルから plugin モデルへ刷新 / credential-patterns ヘッダ更新。
  - レビュー修正: release.yml の `pip install pytest` 欠落 + `git diff` 未スコープ（sync-check.yml と同形に）、テストの Cursor version 未チェック、7ガイドの stale workspace path（`.claude/rules/` `.cursor/skills/` `.gemini/skills/` `@.claude/...`）を「always-on rule」表現へ統一しガード拡張、Gemini 対応矛盾の解消。
- **残作業の統合ロードマッププラン作成**（下記「関連ファイル」）。

## 決定事項
- **P5 スコープ = toolkit リポ内の準備まで**（実 tag リリースと別リポ dev-kit deprecation は P6 へ）。
- **Gemini は保留**（設計 §6.2.4 の runtime 検証後に追加）。README/ガイドは "coming soon"。MVP = CC + Cursor + Codex。
- **初回リリース版 = `v0.1.0`**（manifest は P5 で 0.1.0 にbump 済）。
- **残作業は1つの統合ロードマッププランに集約**（ユーザー選択）。Phase A=runtime 検証（C のゲート）/ B=Gemini / C=リリース実行 / D=dev-kit deprecation / E=issue #6。
- **CLAUDE.md 整理（issue #6）は方針未決で保留**（常時オン→オンデマンドは製品挙動変更のため要ユーザー判断）。

## 捨てた選択肢と理由
- **CLAUDE.md を直接編集して縮小** → 生成物なので不可。`rules/*.md` を直し `sync_derived.py` 再生成が正。issue #6 に記録。
- **P5 を全部 subagent で実装** → handover P3c の教訓どおり、確定内容の書換は subagent が脱線しやすい。**書換系（quickstart/architecture）は私が直接、確定verbatim（test/release.yml/README/version）は subagent** のハイブリッドにした。
- **`/code-review` の "no submodule" 等の記述的言及まで許容** → ガードは厳格（クリーンブレイク）方針。README の "There is no submodule" は語そのものを避けて言い換え（"Nothing to vendor…"）。
- **stale `.claude/rules/` 等を untouched ガイドも含め放置** → 同クラスなので7ガイドまとめて修正し、ガードを拡張して再発防止。

## ハマりどころ
- **S4 ガードの厳格性**: README が説明目的で "submodule" の語を使っただけで RED。記述的言及も禁止トークン扱いになるため言い換えで対応。
- **stale パスの行単位除外の落とし穴**: onboard L102 は同一行に `docs/...` と `org/docs/...` が並び、行単位の除外では漏れる。オカレンス単位の lookbehind/明示チェックが必要（P4 で対処済の知見）。
- **release.yml は tag 専用**なので PR では発火しない（PR CI は sync-check のみ）。YAML 妥当性はローカルに pyyaml 無く目視確認（sync-check.yml と同形なので可）。

## 学び
- **コードレビュー2回（P4・P5）とも、レビューコミット自身の取りこぼし**（onboard は直したが create-recipe を忘れる、等）を `/code-review` が捕捉。確定系の一括変更でも漏れは出る → レビュー必須。
- **テストガードを「全スキル/全ファイル走査」の高度で書く**と、新規・既存問わず同クラスの問題を捕捉でき false confidence を防げる（P4 の bare-path 一般化、P5 の workspace-path ガード）。

## 次にやること
- [ ] **P6 Phase A（runtime 実機検証、リリースのゲート）**: CC/Cursor/Codex に GitHub remote から install → SessionStart 注入 / docs-overlay MCP / `workato_docs_lookup` の allowed-tools ゲート有無（#1）/ credential guard / Codex install CLI 構文（README は未検証）/ Codex SessionStart 対応 / auto-learn の Chrome MCP casing（`mcp__Claude_in_Chrome__*`→`mcp__claude-in-chrome__*`）。詳細は P6 プラン。
- [ ] **P6 Phase C（リリース実行）**: A green 後に `git tag v0.1.0 && git push` → release.yml green 確認 → 各エディタ fresh-install スモーク。
- [ ] **P6 Phase B（Gemini）**: `gemini-extension.json` 追加 → 4manifest 同居検証 → coming-soon 解除（非ブロッカー）。
- [ ] **P6 Phase D**: 別リポ `workato-dev-kit` README に deprecation 告知。
- [ ] **issue #6**: CLAUDE.md/rules 整理の方針決定（`rules/*.md` を編集 → `sync_derived.py` 再生成）。

## 関連ファイル
- `superpowers/2026-06-13-workato-toolkit-p6-runtime-release-roadmap-plan.md` — **次にやる統合ロードマップ**（Phase A–E、[manual]/[code]/[ops] タグ + 合否基準 + 失敗時アクション）。**未 commit**。
- `superpowers/2026-06-07-workato-toolkit-plugin-distribution-design.md` — 設計 spec（§6.1 調査事実 / §6.2 要設計判断 / §6.2.4 Gemini 検証）。**未 commit**。
- `superpowers/2026-06-13-workato-toolkit-p5-release-prep-plan.md` — P5 実装プラン（完了）。**未 commit**。
- `.github/workflows/{sync-check,release}.yml` — CI（PR ドリフト検知 / tag リリース）。release.yml は tag 専用。
- `tests/test_release_p5.py` — P5 ガード（legacy/stale パス・version・release.yml 形・README install）。`_LEGACY` 正規表現が plugin-native 表現を強制。
- `rules/*.md`(9) — 常時ルール真ソース。`CLAUDE.md`/`AGENTS.md`/`GEMINI.md`/`.mdc` は生成物（`scripts/sync_derived.py`）。
- 各 manifest: `.claude-plugin/` `.cursor-plugin/` `.codex-plugin/plugin.json` `.agents/plugins/marketplace.json` `.mcp.json`(`${CLAUDE_PLUGIN_ROOT}`) `.codex.mcp.json`(`${PLUGIN_ROOT}`)。`gemini-extension.json` は未作成。
- GitHub: PR #5（P4・マージ済）/ PR #7（P5・マージ済）/ **issue #6**（CLAUDE.md 整理・OPEN）。

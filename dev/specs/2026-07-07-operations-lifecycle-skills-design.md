---
status: draft
---

# 運用ライフサイクルの完備 — dev→本番→運用の通しユーザストーリー 設計書

- 日付: 2026-07-07（r2 同日改訂: ユーザ指摘「ユーザーストーリーの作り込みが甘い」を受け、
  一行テーブルだったストーリーを、アクター・前提・通しウォークスルー・受け入れ基準・
  失敗系まで含む形に全面書き直し。r1 の S 番号は維持し、深掘りで新たに見つかった
  ストーリーを S5-3 / S6-3 / S7-3 / S8-4 として追加）
- 状態: **draft — ユーザレビュー待ち**（承認まで status を上げない）
- 背景: ユーザ指摘「レシピのスタート、ジョブログ取得などが不足。開発して本番に乗せる
  までのユーザストーリーを描いたか」。既存ストーリー（前 spec
  `2026-07-06-workato-integration-skills-design.md` §2）は「作る」側が厚く
  「動かす・運用する」側が薄い。本書はその欠落フェーズ（テスト実行・稼働操作・診断・
  本番運用）を、**一つの連続したシナリオとして通しで**描き直し、ギャップを再導出する。

## 1. アクターと前提

### 1.1 アクター

| アクター | 役割 | 主に使うもの |
|---|---|---|
| **管理者** | ワークスペース初期化、API キー払出し、昇格ポリシー決定、prod 承認 | `/setup-workspace` `/issue-api-keys`、Workato UI（承認・ロール作成） |
| **開発者** | 仕様策定〜ビルド〜dev での push・テスト・修正、Policy A なら dev→test 昇格 | `/spec`〜`/implement`、`/workato-create`、`/push-project`、`/deploy-project`（dev→test）、（新）`/run-recipes` `/diagnose-jobs` |
| **運用担当** | 昇格後の稼働確認、障害の一次対応、回復の確認 | （新）`/inspect-env`、`/deploy-project`、Workato UI（prod の停止・ジョブ再実行）。小さい org では開発者と同一人物（dev 側の修正はそのとき開発者として `/diagnose-jobs` で行う） |
| **エージェント**（Claude Code + 本プラグイン） | 上記アクターの作業を代行・支援する。**環境ごとに能力が非対称**（下表） | — |

### 1.2 環境と権限の前提（前 spec §5 の帰結。本書の全ストーリーが従う）

| | dev | test（=Staging） | prod |
|---|---|---|---|
| エージェントの書込（push / recipes start・stop / sdk push） | ✅ | ❌（読取専用キー + hook #10 が物理ブロック） | ❌（同左） |
| エージェントの読取（jobs / recipes / pull / deploy status） | ✅ | ✅（読取専用キーで可、実機確認は §6） | ✅（同左） |
| 変更の反映経路 | push | Deploy（dev→test。Policy A ならエージェント実行可） | Deploy（test→prod。実行前にユーザ明示確認、**承認は常に UI の人間**） |
| レシピの起動・停止 | エージェント可 | 人間が UI で | 人間が UI で |

この非対称が運用ストーリーの骨格になる: **dev では「エージェントが直す」、test/prod では
「エージェントは見る・原因を特定する、直すのは dev 経由の再昇格、触るのは人間」**。

### 1.3 障害の検知経路（前提の明確化）

本プラグインは**オンデマンド実行**であり、常駐監視はスコープ外（§4）。したがって障害の入口は:

1. **push 直後**: `/push-project --test` → `/diagnose-jobs` の検証ループ（開発者がその場にいる）
2. **昇格直後**: `/deploy-project` 完了後に `/inspect-env <env>` で稼働確認（運用担当がその場にいる）
3. **定常運用中**: Workato 側のアラート（レシピ停止・ジョブ失敗のメール等）や業務側の気づきを
   **人間が受け取り**、セッションを開いて `/inspect-env` / `/diagnose-jobs` を叩く

(3) を自動化したければ Workato のアラート設定・外部監視の領分。`/inspect-env` は
「人間が定期的に叩く」運用には使える（cron 的な常駐はしない）。

## 2. 通しウォークスルー（一次資料 — ストーリーはここから導出する）

題材: **「Salesforce の商談が Closed Won になったら、会計 SaaS に請求書ドラフトを作成し
Slack に通知する」レシピ 1 本**。トリガーはポーリング型（テスト実行が最も厄介なタイプ）、
コネクション 3 本（Salesforce / 会計 SaaS / Slack）。org は Dev / Staging(TEST) / Prod の
3 環境、昇格ポリシー A（dev→test はエージェント実行可）。

各ステップの右に、担保するストーリー ID（§3）を付す。**太字**は本 spec で新設・変更される箇所。

### Phase 0 — 準備（管理者、初回のみ）

1. `/setup-workspace` — リポジトリ骨格、ヘルパー実体化、秘匿 deny-list、プロファイル登録
2. `/issue-api-keys` — dev=書込可 / test・prod=読取専用の 3 キーを払出し、keyring 登録。
   test/prod プロファイルで push が拒否されることをスモーク確認
3. （任意）`/onboard` — 既存資産からナレッジベース初期化

### Phase 1 — 仕様策定〜ビルド（開発者）

4. `/spec` → `/plan` → `/tasks` → `/implement`（小さければ直接 `/workato-create recipe`）。
   産物: `projects/billing-sync/` にレシピ JSON + コネクション JSON 3 本。
   spec には**期待入出力**（商談レコード → 請求書ドラフトのフィールド対応表）が書かれている
   — Phase 2 のテスト照合（S5-2）はこれを期待値として使う

### Phase 2 — dev へ push してテスト（開発者）【本 spec の中心 1/3】

5. `/push-project --test` — 環境ガード（プロファイル `-dev` 確認）→ 検証 → **新規コネクション
   3 本を先に push** → ユーザに UI での認証を依頼（エージェントには資格情報が無い。人間の作業）
   → 残り資産を push → レシピ起動
6. **テストジョブを能動的に発生させる（S5-1）**: トリガーがポーリング型なので、push しただけでは
   ジョブは流れない。エージェントは
   a. テストデータの準備を依頼する — 「Salesforce sandbox で商談を 1 件 Closed Won にしてください」
      （**S5-3**: 外部システム側の操作はエージェントの手が届かない境界。手順書を出して人間に渡す）
   b. ポーリング間隔ぶん待つ（poll-now 相当の API が有れば即時発火 — §6 OQ）
   c. webhook トリガーであれば curl POST、Workflow App であればフォーム送信依頼、と
      **トリガー型ごとに投入手段が変わる**（§3 S5-1 のマトリクス）
7. **結果を期待値と照合する（S5-2）**: `jobs list` でジョブ検出 → `jobs get` で入出力を取得 →
   Phase 1 の spec のフィールド対応表と突き合わせ。「ジョブが成功した」だけでは合格にしない
   （成功して間違ったデータを作るのが一番怖い）
8. 失敗した場合 → **`/diagnose-jobs`（S7-1, S7-2）**: エラー本文から原因分類
   （datapill 参照 / フィールド不一致 → エージェントが直せる。connection 未認証 / 外部 API 側 →
   人間に引き渡す）→ 直せるものは**既定で 修正 → 再 push（--restart-recipes）→ 再検証 を
   green までループ**（dev 限定。ユーザ確認済み 2026-07-07）
9. green になったら **`/run-recipes` で稼働一覧を確認し（S6-2）、検証専用に動かしたレシピは
   停止する（S6-1）** → git commit + push（`/push-project` は git に触れない — 既存規約）

### Phase 3 — test へ昇格して検証（開発者→運用担当）

10. `/deploy-project preview` → `run --to test` → 昇格チェックリスト
    （test 側コネクション認証・environment properties・**test でのレシピ起動 — いずれも
    人間の UI 作業**。Deploy が稼働状態を引き継ぐかは §6 OQ）→ `status --wait`。
    分岐: deploy が failed で終端 → マニフェスト内容とエラーを報告し、dev へ差し戻す
11. **`/inspect-env test`（S8-1）**: レシピが test で稼働しているか、ジョブが流れて成功して
    いるかを読取専用で確認。ここで test 用のテストデータ投入が再度必要なら手順 6 を test の
    外部システム（sandbox）に対して繰り返す。
    分岐: コネクション未認証で全ジョブ失敗 → チェックリスト消化漏れとして人間へ差し戻し

### Phase 4 — prod へ昇格（運用担当 + 管理者）

12. `/deploy-project run --to prod` — ユーザの明示確認 + **承認は管理者が UI で**（自動化恒久禁止）。
    prod でのレシピ起動も人間の UI 作業。
    分岐: 承認が却下された → 理由を確認し、dev での修正（Phase 2）へ差し戻す
13. `/inspect-env prod`（S8-1）: 初ジョブが流れて成功するまで確認。以後、定常運用へ

### Phase 5 — 定常運用: 本番障害（運用担当）【本 spec の中心 2/3】

14. 数週間後、会計 SaaS の API 仕様変更で prod のジョブが連続失敗。**検知は Workato アラート
    メール**（§1.3 (3)）— 運用担当がセッションを開く
15. **`/inspect-env prod`（S8-2）**: 読取専用で失敗ジョブのエラー本文・失敗率・影響レシピを
    特定し、prod のレシピ定義を scratch へ pull して dev と diff。
    → 「定義は dev と同一。外部 API のレスポンス形式変更が原因」＝**外部仕様変更
    （レシピ側の適応修正が必要）**と分類（一時的なレート制限・先方障害なら引き渡し/回復待ちで、
    以降の手順は不要）
16. **緊急止血の判断（S6-3）**: 失敗ジョブが積み上がるだけなら止めない選択もある。止める
    場合、**エージェントは prod のレシピを止められない**（1.2 の非対称）— レシピ名・ID と
    UI の該当 URL を提示して人間に停止してもらう
17. **hotfix 経路（S8-4）**: dev で修正 → Phase 2 の手順 6〜8 で再現・修正・検証（
    `/diagnose-jobs` のループ）→ `/deploy-project run --to test` → `/inspect-env test` →
    `run --to prod`（確認+承認）→ `/inspect-env prod` で回復確認。
    **prod を直接直す近道は存在しない**（キーと hook が物理的に塞いでいる）
18. **取りこぼしの回収（S7-3）**: 障害期間中に失敗したジョブの再実行。rerun API が有るかは
    §6 OQ — 無ければ「UI のジョブ再実行手順 + 対象ジョブ一覧の提示」まで がエージェントの仕事
19. 復旧後、`/learn-recipe` / org/docs へ「この connector はレスポンス形式が変わりうる」等の
    知見を書く — 学習ループが閉じる

### Phase 6 — 昇格自体が悪かった場合（S8-3、Phase 5 の変形）

20. 新バージョンを昇格した直後に業務エラー多発 → 原因は新バージョン自体 → **ロールバック**:
    Deploy API に「過去 deployment の再適用」が有るかは §6 OQ。無い場合の正規手順は
    **git 上の直前リリース状態を dev に復元 → 通常経路で再昇格**（git がリリース台帳になる。
    Phase 2 の手順 9 で commit している事がここで効く）

## 3. ストーリー詳細（受け入れ基準つき）

記法: 各ストーリーは「アクター / 前提 / トリガー / 主フロー / 受け入れ基準（AC）/
失敗・分岐 / 現状とギャップ」。現状表記: ✅=充足 / △=手段はあるがワークフロー未整備 / ❌=手段なし。

### S5-1 テストジョブを能動的に発生させたい

- **アクター**: 開発者（ウォークスルー手順 6）
- **前提**: `/push-project` で dev に push 済み、レシピ起動済み、コネクション認証済み
- **トリガー**: `--test` 指定、または push 完了後にテストへ進むと決めた時
- **主フロー**: トリガー型を recipe JSON の trigger adapter から判定し、型別に投入する:

| トリガー型 | 投入手段 | 人間の作業か |
|---|---|---|
| webhook（realtime） | webhook URL へ curl POST（URL の機械的取得可否は §6 OQ） | エージェント可 |
| ポーリング | ソース側にテストデータ作成を依頼 → 次回ポーリング待ち（poll-now の有無は §6 OQ） | **依頼（S5-3）** |
| スケジュール | 次回実行を待つ／一時的に間隔を縮めて push（戻し忘れ防止の注意書き必須） | エージェント可 |
| Workflow App フォーム | フォーム送信を依頼（URL 提示） | **依頼** |
| MCP サーバ / Genie | AI クライアントからの呼出しを依頼（MCP サーバは push-project §4 の既存チェックリスト。Genie 用チェックリストは未整備 — §4 の /push-project 改修で新設） | **依頼** |

- **AC**:
  - [ ] 5 つのトリガー型すべてに対し「何をすればジョブが 1 件流れるか」が手順化されている
  - [ ] 人間に依頼する場合、コピペで実行できる具体的指示（URL・レコード条件つき）を出す
  - [ ] 投入後、ジョブ出現まで待機し S5-2 の照合へ自動で接続する
- **失敗・分岐**: ジョブが一定時間出現しない → レシピ停止中/トリガー条件不一致/ポーリング
  未到達を切り分けて報告（無限待ちしない）
- **現状**: ❌ `--test` は「失敗ジョブの有無を見る」だけ。投入手段が未整理

### S5-2 テスト結果を期待値と照合したい

- **アクター**: 開発者
- **前提**: S5-1 でジョブが 1 件以上流れた。spec（または依頼内容）に期待入出力がある
- **主フロー**: `jobs get` で入出力を取得 → 期待値（spec のフィールド対応表）と突合 →
  相違点を「ジョブは成功したが値が違う」を含めて報告
- **AC**:
  - [ ] 「ジョブ成功 = テスト合格」としない。出力フィールドの値まで照合する
  - [ ] 期待値が無い場合、照合前にユーザへ期待値を確認する（勝手に合格させない）
  - [ ] 不一致は S7-1 の原因分類に接続する
- **失敗・分岐**: 出力が暗号化/マスクされ取得不能 → その旨と UI での確認手順を提示
- **現状**: △ `jobs get` はあるが照合ワークフローなし

### S5-3 テストデータの準備は外部システム側の作業（境界の明示）【新設】

- **アクター**: 開発者（依頼を受けるのは人間、またはソース側 SaaS の別ツール）
- **内容**: ソースシステム（例: Salesforce sandbox）へのテストレコード作成は Workato の
  API では代行できない。エージェントは**作成すべきレコードの条件（トリガー条件を満たす
  フィールド値）を具体化して依頼する**ところまでが仕事
- **AC**: [ ] 依頼文にトリガー条件を満たす具体値が含まれる / [ ] 作成後の合図で S5-1 の待機を再開する
- **現状**: ❌ ストーリー自体が無かった（r1 では S5-1 に暗黙に混ざっていた）

### S6-1 push とは独立にレシピを起動/停止/再起動したい（dev）

- **アクター**: 開発者
- **前提**: dev プロファイル。対象は単独レシピ or プロジェクト一括
- **トリガー**: 修正確認のための再起動、検証終了後の停止、push を伴わない稼働変更
- **主フロー**: `/run-recipes start|stop|restart --id <id> | --all | --project <name>` →
  ヘルパー `recipes start/stop`（dev ガード内蔵）→ 実行後に稼働状態を再表示して確認
- **AC**:
  - [ ] 起動・停止・再起動が push なしで実行でき、実行後の状態が表示される
  - [ ] restart は stop→start の順序保証つき（更新反映の定番操作として一級化）
  - [ ] test/prod プロファイル下では実行せず、S6-3 の案内に切り替わる
- **失敗・分岐**: start 失敗（コネクション未認証等）→ エラー本文と対処を表示
- **現状**: △ `/push-project --start` のフラグとヘルパーに埋没。一級のスキルなし

### S6-2 プロジェクトのレシピ稼働状態を一覧したい

- **アクター**: 開発者・運用担当
- **主フロー**: `/run-recipes`（既定 = status）→ `recipes list` を「名前 / ID / running・
  stopped / 直近ジョブの成否」の表に整形
- **AC**: [ ] 停止中レシピが一目で分かる / [ ] S6-1 の操作対象 ID がそのまま使える
- **現状**: △ `recipes list` の生出力を読むだけ

### S6-3 本番のレシピを止めたい（エージェントは代行しない — 境界の明示）【新設】

- **アクター**: 運用担当（実操作は人間）
- **前提**: prod で障害進行中、止血判断が下った（ウォークスルー手順 16）
- **主フロー**: `/run-recipes stop` が test/prod プロファイル下で呼ばれた場合、hook #10 が
  物理ブロックする**前に**スキル自身が検知し、(a) なぜ代行できないか（1.2 の権限設計）、
  (b) 対象レシピ名・ID、(c) UI の該当画面 URL、(d) 停止後の hotfix 経路（S8-4）を提示する
- **AC**:
  - [ ] エージェントが prod の start/stop を試みない（hook 発火はエラーではなく設計通りだが、
        スキルは hook に到達する前に案内へ切替える）
  - [ ] 人間が UI で止めるのに必要な情報（レシピ特定 + URL）が揃って出力される
- **現状**: ❌ ストーリー自体が無かった。「hook が拒否する」だけでは運用担当は次の一手が分からない

### S7-1 失敗ジョブを検知→原因分類→修正提案まで一気にやってほしい（dev）

- **アクター**: 開発者
- **前提**: dev。S5-1 の投入直後、または `jobs list --status failed` に失敗がある状態
- **主フロー**: `/diagnose-jobs` → 失敗ジョブ収集（ヘルパーの `jobs list` は**レシピ単位・
  `--recipe-id` 必須** — プロジェクト/フォルダ指定・無指定時は `recipes list --folder-id`
  （.workatoenv の folder_id）でレシピを列挙し、レシピごとに `jobs list --status failed` を
  照会する）→ 各ジョブ `jobs get` でエラー本文 → 原因を分類:

| 分類 | 例 | エージェントが直せるか |
|---|---|---|
| datapill 参照エラー | 存在しない path 参照 | ✅ recipe JSON 修正 |
| フィールド不一致 | フィールド名/UUID 誤り | ✅ 同上 |
| connection 未認証・失効・権限不足 | 401/403、認証切れ | ❌ UI 認証・権限見直しを依頼 |
| 外部仕様変更 | 先方 API のレスポンス形式・スキーマ変更 | ✅ レシピを適応修正（prod 起因なら S8-4 の経路で） |
| 外部の一時障害 | レート制限・先方ダウン | ❌ 分析結果を添えて引き渡し（回復待ち） |
| 期待値不一致（S5-2 由来） | 成功したが値が違う | ✅ マッピング修正 |

- **AC**:
  - [ ] 分類ごとに「直せる/直せない」と次アクションが明示される
  - [ ] 同一原因の失敗ジョブ N 件は 1 つに束ねて報告する（1 件ずつ N 回報告しない）
  - [ ] `tail` モードで新規失敗の追従監視ができる（セッション内・明示的な終了条件つき —
        常駐監視にはしない（§1.3）。終了条件の実挙動は実機チェックリスト §7 へ）
- **現状**: △ CLI 止まり。診断ワークフローなし（r1 で /diagnose-jobs 見送りの判断を覆した — 維持）

### S7-2 修正→再 push→再起動→再検証のサイクルを回してほしい（dev）

- **アクター**: 開発者
- **前提**: S7-1 で「直せる」分類が出た。**dev 限定**
- **主フロー**（既定。ユーザ確認済み 2026-07-07）: 修正 → `workato push --restart-recipes`
  → S5-1 の手段で再投入（人間依頼が必要な型なら依頼して待つ）→ S5-2 照合 → 失敗なら
  次の仮説へ。**green になるまでループ**。`--no-fix` で診断・提案のみに抑制
- **AC**:
  - [ ] ループの各周回で「何を直したか / 結果どうなったか」を 1 行ずつ記録し、最後にまとめて報告
  - [ ] 同一仮説での空回りを検知して止まる（同じ修正を 2 度提案しない）
  - [ ] ループに上限がある（既定 5 周で停止しエスカレート。ポーリング型は 1 周ごとに
        人間へのテストデータ依頼を消費するため、上限は特に重要）
  - [ ] ループ終了時（green）に git commit を促す（Phase 2 手順 9）
- **失敗・分岐**: 直せない分類に転じた（実は認証切れだった等）→ ループを止めて引き渡し
- **現状**: △ push-project §7 に手順記述のみ。ループの主体・終了条件・記録が未定義

### S7-3 修正後、障害期間中に失敗したジョブを回収したい（replay/backfill）【新設】

- **アクター**: 開発者・運用担当
- **前提**: S7-2（dev）または S8-4（prod hotfix）で修正が反映済み。修正**前**に失敗した
  ジョブが残っている（＝業務データが未処理のまま）
- **主フロー**: 失敗ジョブ一覧（期間・件数）を提示 → rerun API が有れば dev では代行実行、
  無ければ **UI のジョブ再実行手順 + 対象一覧**を提示（§6 OQ）。test/prod は常に人間が UI で
  — **prod 側の対象一覧の作成（読取専用）は `/inspect-env` が担い**、dev 側は `/diagnose-jobs`
  が green 到達時に必ず指摘する
- **AC**:
  - [ ] 「レシピが green になった」で終わらず、未処理ジョブの存在を必ず指摘する
  - [ ] 再実行で二重処理が起きるケース（非冪等なレシピ）を警告する
- **現状**: ❌ ストーリー自体が無かった。ヘルパーに rerun 系コマンドなし

### S8-1 昇格後、業務が実際に流れているか確認したい（test/prod、読取専用）

- **アクター**: 運用担当
- **前提**: `/deploy-project` の deploy が succeeded。**deploy 成功 ≠ 業務が動く**
  （コネクション未認証・properties 未設定・レシピ停止中でも deploy は成功する）
- **トリガー**: 昇格直後（ウォークスルー手順 11, 13）、または定期の健全性確認
- **主フロー**: `/inspect-env <test|prod>` → (a) レシピ稼働状態一覧、(b) 直近 N 件の
  ジョブ成否サマリ、(c) 失敗があればエラー本文の要約、(d) 昇格チェックリスト残項目の疑い
  （全滅パターン → 認証/properties を疑う）を報告
- **AC**:
  - [ ] 読取専用キーだけで完結する（書込系コマンドを一切呼ばない）
  - [ ] 「deploy 成功だがレシピ停止中/初ジョブ未達」を明確に区別して報告する
  - [ ] 初ジョブが必要なら S5-1 のマトリクスを test/prod の文脈で提示する（投入は人間）
- **現状**: ❌

### S8-2 本番障害を読取専用で調査し、dev と diff したい

- **アクター**: 運用担当
- **前提**: §1.3 (3) の経路で障害を検知した。prod キーは読取専用
- **主フロー**: `/inspect-env prod` → 失敗ジョブの範囲特定（いつから・何件・どのレシピ）→
  エラー本文 → prod の定義を **scratch ディレクトリへ** pull（ワークスペースの dev 資産を
  汚さない。ヘルパーに `pull` サブコマンドは無い — 実現形は `workato pull` を prod
  プロファイル + scratch 内の `projects use` で実行、読取専用キーで通るかは §6 OQ）→
  dev と diff → 「定義差・レシピ欠陥」「環境差（認証・properties・起動状態）」
  「外部仕様変更（適応修正が必要 → S8-4）」「外部の一時障害（引き渡し・回復待ち）」に分類
  → S6-3（止血）/ S8-4（hotfix）/ 引き渡しへ接続
- **AC**:
  - [ ] 調査の全過程で書込系コマンドが発生しない
  - [ ] pull 先が scratch であり、dev のワークスペースが変更されない
  - [ ] 分類結果と推奨の次アクション（止血する/しない、hotfix 対象）が提示される
- **現状**: △ `pull --profile` の記述のみ（前 spec S3-5 のまま未実装）

### S8-3 悪い昇格を戻したい（ロールバック）

- **アクター**: 運用担当 + 管理者（prod 承認）
- **前提**: 新バージョン自体が原因と特定済み（S8-2）。git に直前リリース状態がある
- **主フロー**: Deploy API に過去 deployment 再適用が有れば準正規経路として検討（§6 OQ）。
  無い場合の正規手順: **git で直前リリース状態を dev に復元 → 通常昇格経路で再昇格**
  （dev→test→prod。近道なし）。`/deploy-project` に checklist として手順化
- **AC**:
  - [ ] 「git がリリース台帳」という前提が checklist に明記される（push 時 commit の徹底が上流条件）
  - [ ] ロールバック中も prod 承認は人間（通常昇格と同じガード）
- **失敗・分岐**: git に直前状態が無い（commit 漏れ）→ prod から scratch へ pull した定義を
  ベースに復元するリカバリ手順（最後の手段、要ユーザ確認）
- **現状**: ❌ ストーリー自体が存在しなかった

### S8-4 本番障害の修正は dev 経由で最短昇格したい（hotfix 経路）【新設】

- **アクター**: 開発者 + 運用担当
- **前提**: S8-2 で「定義差・レシピ欠陥」または「外部仕様変更（適応修正が必要）」と分類された
- **主フロー**: ウォークスルー手順 17。dev で再現（可能なら）→ `/diagnose-jobs` ループ →
  昇格（dev→test→prod、チェックリストは省略しない）→ `/inspect-env` で回復確認 →
  S7-3 の回収へ
- **AC**:
  - [ ] 「prod を直接直す」選択肢が一切提示されない
  - [ ] 通常昇格と同じガード・チェックリストを通る（緊急時ほど省略しがちな箇所を checklist が支える）
  - [ ] 回復確認（S8-1）と取りこぼし回収（S7-3）まで含めて「完了」とする
- **現状**: △ 各部品はあるが、経路として文書化されていない

## 4. ギャップ → 追加・変更するもの

### 新スキル（3）

| スキル | 担うストーリー | 概要 |
|---|---|---|
| **`/run-recipes`** | S6-1, S6-2, S6-3 | `status`（既定・稼働一覧）/ `start` / `stop` / `restart`、`--id <id>` / `--all` / `--project <name>`。実行はヘルパー `recipes start/stop`（dev ガード内蔵）経由。**test/prod プロファイル下では実行前に検知して S6-3 の案内（理由 + レシピ特定情報 + UI URL + hotfix 経路）に切替える**（hook #10 は最終防衛線であり UX ではない） |
| **`/diagnose-jobs`** | S7-1, S7-2, S7-3（dev 側）, S5-2 | 失敗ジョブ収集（フォルダ→`recipes list`→レシピごと `jobs list --status failed`）→ `jobs get` → 原因分類（S7-1 の表）→ **既定で修正→再 push（`--restart-recipes`）→再投入・再照合を green までループ**（dev 限定・上限つき）。`--no-fix` で診断・提案のみ。ループ記録・空回り検知・同一原因の束ね報告。**green 後に未回収の失敗ジョブ（S7-3）を必ず指摘**。`tail` モードで追従監視（セッション内・終了条件つき） |
| **`/inspect-env`** | S8-1, S8-2, S7-3（prod 側） | `<test\|prod>` を対象に**読取専用**で: レシピ稼働状態、直近 N 件ジョブ健全性、失敗のエラー本文要約、prod 定義を **scratch へ pull** → dev と diff、原因の 4 分類（定義差・レシピ欠陥/環境差/外部仕様変更/外部一時障害）と次アクション提示。障害復旧後は**回収対象の失敗ジョブ一覧 + UI 再実行手順**を提示（S7-3 の prod 側 — 実行は常に人間）。書込系コマンド不使用をスキル冒頭で宣言。読取専用キー（/issue-api-keys の test/prod キー）前提 |

### 既存の変更（2）

| 対象 | 変更 |
|---|---|
| `/push-project` | `--test` を S5-1/S5-2/S5-3 対応に強化: **トリガー型別テスト投入マトリクス**（§3 S5-1 の表）、人間依頼テンプレート（具体値つき）、投入→照合（期待値突合）→`/diagnose-jobs` への接続。起動系の記述は `/run-recipes` への参照に置換（重複を持たない） |
| `/deploy-project` | S8-3 の**ロールバック checklist** を追加（git 台帳前提の明記、API ロールバックの有無は §6 OQ、commit 漏れ時のリカバリ）。S8-4 の hotfix 経路を注記（「緊急時もチェックリストを省略しない」） |

### ヘルパー拡張（候補 — §6 OQ の回答次第）

| 候補 | 対応ストーリー | 条件 |
|---|---|---|
| `jobs rerun`（失敗ジョブ再実行） | S7-3 | Developer API に endpoint が有る場合のみ。dev ガード内蔵 |
| `recipes poll-now`（ポーリング即時発火） | S5-1 | 同上 |
| `deploy status --wait`（終端まで追跡） | 昇格フロー（手順 10） | **無条件で実装** — deploy-project スキル文書は既に `status <id> [--wait]` を記述しているがヘルパー未実装（レビューで発見した既存乖離）。追随させる |

### スキル化を見送るもの（理由つき）

- 常駐監視・定期ヘルスチェック — プラグインはオンデマンド実行が前提（§1.3）。恒常監視は
  Workato 側のアラート/外部監視の領分（`/inspect-env` を人間が定期に叩く運用は可）
- connection 管理 / properties — 前 spec の判断を維持（CLI で充足）
- テストデータ生成の自動化（外部 SaaS への書込） — S5-3 の境界の外。ソース側 SaaS の
  MCP/ツールが別途あればユーザがそれで行う。本プラグインは依頼の具体化まで

## 5. 決定事項と前提（レビューで覆す前提で明文化）

1. **自動修正の範囲（ユーザ確認済み 2026-07-07）**: `/diagnose-jobs` は**既定で自動修正と
   再 push まで実行**する（dev 限定）。直せない分類（connection 認証・権限不足、
   外部の一時障害 — S7-1 の表）はユーザーへ引き渡す。診断のみは `--no-fix`。test/prod への修正は常に deploy 経由
   （hook #10 が物理的に下支え）
2. **読取専用の担保は 3 層**: スキル記述（書込コマンド不使用）+ 権限（test/prod キーは
   読取専用 — /issue-api-keys のマトリクス）+ hook #10 の物理ブロック
3. **境界では「案内」まで が仕事**: エージェントが代行できない操作（UI 認証、フォーム送信、
   prod の停止、テストデータ作成、prod 承認）は、**具体的な対象・URL・手順を揃えて人間に
   渡す**ところまでをスキルの責務とする（S5-3 / S6-3 / S8-3 の AC）
4. **「deploy 成功」「ジョブ成功」を合格にしない**: 業務の稼働確認は S8-1、値の照合は S5-2 が
   定義する（成功して間違ったデータを作るケースを合格にしない）
5. スキル名は動詞-名詞規約: `/run-recipes` `/diagnose-jobs` `/inspect-env`（#16 の接頭辞判断
   が出たら追従）
6. S5-1 のテスト投入は**独立スキルにしない**（push 直後の文脈に強く従属するため
   `/push-project --test` の一部。`/diagnose-jobs` の再投入からも同じマトリクスを参照する）

## 6. Open Questions（実機で確認 — issue #32 Phase A と同じセッションで）

- [ ] Deploy API にロールバック（過去 deployment の再適用）があるか（S8-3）
- [ ] ポーリングトリガーを即時発火させる手段（`poll now` 相当）の有無（S5-1）
- [ ] webhook トリガーの URL 取得方法（recipe 定義から機械的に得られるか）（S5-1）
- [ ] `jobs list` / `jobs get` / `workato pull` が test/prod の読取専用キーで通るか（S8-1, S8-2）
- [ ] 失敗ジョブの再実行（rerun/resume）API の有無（S7-3）
- [ ] `jobs get` でジョブの入出力データがどこまで取得できるか（マスクの有無）（S5-2）
- [ ] Deploy が対象レシピの稼働状態を引き継ぐ/開始するか（S8-1 — 引き継がないなら
      昇格チェックリストの「レシピ起動は人間の UI 作業」が常に必要）

## 7. テスト戦略（AC → ガードの写像）

- 既存流儀: スキル構造ガード（EXPECTED_SKILLS、frontmatter、MCP 言及、bare パス禁止）は自動適用
- `/run-recipes`: 「ヘルパー経由で実行」「raw CLI の start/stop を書かない」+ **S6-3 節の存在**
  （test/prod 検知時の案内 — 「UI」「hotfix」への言及）ガード
- `/diagnose-jobs`: 「修正サイクルは dev 限定」「test/prod には触れない」文言 + `--no-fix` +
  **S7-3（未回収ジョブの指摘）と空回り検知の記述**ガード
- `/inspect-env`: 「書込系コマンド（push / recipes start・stop / deploy run / sdk push）を
  含まない」全走査ガード + **scratch へ pull する記述**の存在
- `/push-project`: **トリガー型マトリクス（5 型）の存在**ガード
- `/deploy-project`: **ロールバック checklist（「git」台帳前提の文言）と hotfix 注記の存在**ガード
- ヘルパー拡張が入る場合（rerun / poll-now / `deploy status --wait`）は tests/helper/ にユニット追加
- ストーリーの AC のうち実行時挙動（ループ記録・上限、束ね報告、`tail` の終了条件等）は
  静的ガード不能 — issue #32 Phase A の実機チェックリストに転記する

## 8. 実装順

1. 本 spec のユーザレビュー（**status: draft → active はレビュー後**）
2. `/run-recipes`（最小・独立、S6-3 の案内含む）→ 3. `/diagnose-jobs` → 4. `/inspect-env`
   → 5. push-project / deploy-project の改修（S5-1 マトリクスは実装可。poll-now / webhook URL /
   rerun / rollback の各分岐は §6 の実機回答後に確定）
